spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/reservation?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: kms123!!
  jpa:
    show-sql: true
    database: mysql
    hibernate:
      ddl-auto: update
  mail:
    username: crg2ch@gmail.com
    password: vgbsjgclbmaxrlni
    host: smtp.gmail.com
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html